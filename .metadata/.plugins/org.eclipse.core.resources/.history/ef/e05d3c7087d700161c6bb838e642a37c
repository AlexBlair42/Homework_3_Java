import javax.swing.JComponent;
import java.awt.*;
import javax.swing.*;
import java.awt.event.*;
import java.net.URL;

public class UI extends JComponent {
JFrame frame;
	
	public UI() 
	{
		initialize();
		
		frame.setVisible(true);
	}
	
	private void initialize()
	{
		frame = new JFrame();
		frame.setBounds(100,100,450,300);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		
		frame.setLayout(new GridBagLayout());
		
		try
		{
			Toolkit toolkit = Toolkit.getDefaultToolkit();
			URL imageURL = getClass().getResource("/resource/PoliceStation.jpg");
			Image img = toolkit.getImage(imageURL);
			img = img.getScaledInstance(100, 100, Image.SCALE_SMOOTH);
			ImageIcon icon = new ImageIcon(img);
			addgb(new JButton(icon),30,60);
		}
		catch(Exception ex){;}
		
		try
		{
			Toolkit toolkit = Toolkit.getDefaultToolkit();
			URL imageURL = getClass().getResource("/resource/school.png");
			Image img = toolkit.getImage(imageURL);
			img = img.getScaledInstance(100, 100, Image.SCALE_SMOOTH);
			ImageIcon icon = new ImageIcon(img);
			addgb(new JButton(icon),0,60);
		}
		catch(Exception ex){;}
	}
	
	private void addgb(Component c, int x, int y)
	{
		GridBagConstraints gbc = new GridBagConstraints();
		gbc.gridx = x;
		gbc.gridy = y;
		frame.add(c,gbc);
	}
	
	public static void main(String[] args)
	{
		UI u = new UI();
	}
}
